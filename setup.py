import sys
from distutils.core import setup


setup(
    name='Simulation-based inference via telescoping ratio estimation for trawl processes',
    version='1.0',
    author='Author',
    description='supporting code',
    install_requires=['matplotlib', 'numpy', 'pandas', 'seaborn', 'tqdm']
)
